# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: goboden-tenant-admin
spec:
  interval: 30m
  chartRef:
    kind: OCIRepository
    name: app-template
  install:
    remediation:
      retries: -1
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  values:
    controllers:
      goboden-tenant-admin:
        strategy: RollingUpdate
        containers:
          app:
            image:
              repository: registry.system.svc.cluster.local:5000/goboden-tenant-admin
              tag: main
              digest: "sha256:7f5d5472a40c77a5d6cae354283bb5a5872f4e1f0acae2a8c2791021a8e373cd" # {"$imagepolicy": "flux-system:goboden-tenant-admin:digest"}
            ports:
              - name: http
                containerPort: &port 80
            resources:
              requests:
                cpu: 5m
                memory: 16Mi
              limits:
                memory: 64Mi
    service:
      app:
        ports:
          http:
            port: *port
    route:
      app:
        hostnames: ["*.goboden.no"]
        parentRefs:
          - name: internal
            namespace: kube-system
            sectionName: https-goboden-wildcard
        rules:
          - matches:
              - path:
                  type: PathPrefix
                  value: /admin/
            backendRefs:
              - identifier: app
                port: *port
